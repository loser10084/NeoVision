你现在这条路线已经很接近“正确答案”了：**问题的核心不是 .mha 解析，而是 WebGL 的深度/混合状态**。

你目前用 **同一批点（同一 draw call）**，靠 `aMask` 在片元里区分脑点/肿瘤点。这样会导致一个致命限制：

> **`gl.depthMask()`（是否写深度）是“整个 draw call 级别”的状态，不能对脑点/肿瘤点分别设置。**
> 所以只要你这次 draw 开了深度写入，脑点就会把深度缓冲“占满”，肿瘤点（即使画红色）也会被深度测试挡住。

解决办法：**改成“两次绘制（two-pass）”**，同一 VBO 也行，只要分两次 draw，并在 shader 里 `discard` 过滤即可。

------

## 你要的最佳实践：两次 draw + 不同 depthMask + blending

### WebGL 状态建议（通用）

```js
gl.enable(gl.DEPTH_TEST);
gl.depthFunc(gl.LEQUAL);

gl.enable(gl.BLEND);
gl.blendFunc(gl.SRC_ALPHA, gl.ONE_MINUS_SRC_ALPHA); // 标准透明
```

------

## Pass 1：先画肿瘤（写深度，基本不透明）

- 目的：让肿瘤在深度上“站住”，后面脑点不去覆盖它
- 透明策略：可不透明（alpha=1），或者稍微透明但仍写深度

```js
gl.depthMask(true);        // ✅ 写深度
gl.enable(gl.BLEND);       // 可开可关（肿瘤一般可不透明）
setUniform(uPass, 1);      // 1=肿瘤pass
drawPoints();
```

------

## Pass 2：再画脑（半透明 + 不写深度）

- 目的：脑作为“透明外壳”，但不把深度写进去，否则又遮内部
- 关键：`gl.depthMask(false)`

```js
gl.depthMask(false);       // ✅ 关键：不写深度
gl.enable(gl.BLEND);
setUniform(uPass, 0);      // 0=脑pass
drawPoints();
gl.depthMask(true);        // 画完恢复
```

------

## Shader：用 uPass + vMask 做 discard（最省改动）

你现在已经有 `aMask`，只需要加一个 `uPass`。

### Vertex shader（要把 mask 传下去）

WebGL2 推荐 `flat`（避免插值），WebGL1 不支持 `flat` 也没事（点精灵不会跨顶点插值）：

```glsl
// WebGL2:
layout(location=0) in vec3 aPos;
layout(location=1) in float aMask;

flat out float vMask;

void main() {
  vMask = aMask;
  gl_Position = ...;
  gl_PointSize = ...;
}
```

### Fragment shader（两次 pass 分别输出）

```glsl
precision mediump float;
flat in float vMask;   // WebGL1 删掉 flat

uniform int uPass;     // 0=brain, 1=tumor
uniform float uBrainAlpha; // 例如 0.08~0.25

out vec4 outColor;

void main() {
  if (uPass == 1) {            // tumor pass
    if (vMask < 0.5) discard;
    outColor = vec4(1.0, 0.0, 0.0, 1.0);
  } else {                     // brain pass
    if (vMask > 0.5) discard;
    outColor = vec4(1.0, 1.0, 1.0, uBrainAlpha);
  }
}
```

> 这样你不用拆 VBO、不用建两套点云；**两次 draw + discard** 就能做到“肿瘤清晰、脑半透明不遮挡”。

------

## 如果你希望“肿瘤永远可见”（更像叠加标注）

有些临床交互希望肿瘤不被任何东西挡住，那就把肿瘤作为最后一遍画，并关闭深度测试：

```js
// 先画脑：depthTest=true, depthMask=false, blending=true
// 最后画肿瘤：
gl.disable(gl.DEPTH_TEST);
gl.enable(gl.BLEND);
gl.blendFunc(gl.SRC_ALPHA, gl.ONE); // 可选：加亮（additive）
setUniform(uPass, 1);
drawPoints();
gl.enable(gl.DEPTH_TEST);
```

视觉会更“标注化”，但不是真实遮挡关系——看你产品要哪种。

------

## 你现在点云“雾很大”的补救（不改渲染管线）

半透明点云叠太多会发灰，移动端尤其明显。你可以在重建时把“脑点”变成更像外表面的壳：

**只保留边界点（surface points）**：某 voxel 满足阈值，但 6 邻域/26 邻域里存在低于阈值的邻居 → 判定为表面点。
这样脑点云天然是外壳，肿瘤在内部就更容易看到，点也少很多，性能更稳。

------

## uni-app App 端注意点（WebView/WebGL）

- 如果你在 `<web-view>` 里渲染：上面这些 WebGL 状态都正常可用
- 如果你是 `renderjs` + canvas：也一样，但注意不同端对 WebGL1/2 支持差异，尽量兼容 WebGL1 写法（不依赖 `flat`、MRT）

------

如果你把你当前的 **渲染循环那段（gl state + draw）** 和 **shader** 贴一小段，我可以直接按你代码风格给出“最小 diff”：该加哪个 uniform、在哪两次 draw、以及 brainAlpha 推荐范围（通常 0.08~0.18 比较舒服）。