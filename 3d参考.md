# 3D 渲染参考

本文档说明 3D 界面渲染逻辑、技术方法与使用方式，适用于 `MasterImage_frontend/pages/model/viewer.vue`。

## 渲染逻辑
- 数据来源
  - 主体体数据：Flair MHA（`/static/brats_2013_pat0001_1_Flair.mha`）
  - 标签体数据：Label MHA（`/static/brats_2013_pat0001_1_label.mha`）
  - 要求：两者维度一致，否则仅渲染主体体数据。
- 点云构建
  - 对主体体数据按阈值采样，生成半透明“脑”点云。
  - 对标签体数据按 label 值（1-4）采样，生成显眼的标签点云。
  - 采样采用 stride 限制点数，避免过载：
    - 标签点云占用约 25% 预算（至少 20000）。
    - 剩余用于主体体数据。
- 两次绘制（two-pass）
  - Pass 1：绘制标签点云（depthMask=true），保证标签可见。
  - Pass 2：绘制主体点云（depthMask=false，alpha 可调），作为半透明外壳。

## 技术方法

- WebGL 渲染管线
  - `gl.enable(gl.DEPTH_TEST)` + `gl.depthFunc(gl.LEQUAL)`
  - `gl.enable(gl.BLEND)` + `gl.blendFunc(gl.SRC_ALPHA, gl.ONE_MINUS_SRC_ALPHA)`
- Shader（核心逻辑）
  - 顶点数据结构（每点 5 个 float）：
    - `aPosition.xyz`：归一化坐标
    - `aIntensity`：主体灰度（仅主体点用）
    - `aLabel`：标签值（0 表示非标签）
  - 片元着色器：
    - `uPass == 1` 仅绘制 `aLabel > 0` 的点，并按 label 分色。
    - `uPass == 0` 仅绘制非标签点，并使用 `uBrainAlpha` 控制透明度。
- 标签颜色（显眼配色）
  - label 1：亮红 `#ff3333`
  - label 2：亮青 `#00e6ff`
  - label 3：亮黄 `#fff200`
  - label 4：亮紫 `#e600ff`
- MHA 解析
  - 支持 `MET_SHORT / MET_USHORT / MET_INT / MET_FLOAT`。
  - 压缩数据优先使用 `DecompressionStream`，失败时回退到 `inflateRaw`。

## 使用说明
- 进入 3D 页面（H5/WebView 环境）
  - App 侧无法直接使用原生 WebGL canvas，需使用 H5/WebView。
- 操作入口
  - `Load sample MHA`：加载默认 Flair + Label 样例。
  - `Load from study`：加载后端返回的模型地址（`modelUrl`）。
  - `Pick MHA`：H5 选择本地文件（仅单体数据）。
- 交互说明
  - 鼠标拖拽/单指拖动：旋转
  - 滚轮/双指捏合：缩放
- 参数调节
  - `Threshold`：主体体阈值（提高可减少噪点）
  - `Point size`：点大小
  - `Opacity`：主体体透明度
  - `Max points`：点数上限
  - `Rebuild`：基于当前参数重新采样点云
  - `Reset view`：重置视角

## 调整建议
- 标签不够醒目：可在片元 shader 中提高亮度或更换配色。
- 主体雾化严重：提高阈值或降低 `Opacity`，也可降低 `Max points`。
- 性能不足：提高采样 stride 或降低 `Max points`。
