# 3D实现(后续有界面优化,切面，热力图)

## Pass 1：先画肿瘤（写深度，基本不透明）

- 目的：让肿瘤在深度上“站住”，后面脑点不去覆盖它
- 透明策略：可不透明（alpha=1），或者稍微透明但仍写深度

```js
gl.depthMask(true);        // ✅ 写深度
gl.enable(gl.BLEND);       // 可开可关（肿瘤一般可不透明）
setUniform(uPass, 1);      // 1=肿瘤pass
drawPoints();
```

------

## Pass 2：再画脑（半透明 + 不写深度）

- 目的：脑作为“透明外壳”，但不把深度写进去，否则又遮内部
- 关键：`gl.depthMask(false)`

```js
gl.depthMask(false);       // ✅ 关键：不写深度
gl.enable(gl.BLEND);
setUniform(uPass, 0);      // 0=脑pass
drawPoints();
gl.depthMask(true);        // 画完恢复
```

------

## Shader：用 uPass + vMask 做 discard

你现在已经有 `aMask`，只需要加一个 `uPass`。

### Vertex shader（要把 mask 传下去）

WebGL2 推荐 `flat`（避免插值），WebGL1 不支持 `flat` 也没事（点精灵不会跨顶点插值）：

```glsl
// WebGL2:
layout(location=0) in vec3 aPos;
layout(location=1) in float aMask;

flat out float vMask;

void main() {
  vMask = aMask;
  gl_Position = ...;
  gl_PointSize = ...;
}
```

### Fragment shader（两次 pass 分别输出）

```glsl
precision mediump float;
flat in float vMask;   // WebGL1 删掉 flat

uniform int uPass;     // 0=brain, 1=tumor
uniform float uBrainAlpha; // 例如 0.08~0.25

out vec4 outColor;

void main() {
  if (uPass == 1) {            // tumor pass
    if (vMask < 0.5) discard;
    outColor = vec4(1.0, 0.0, 0.0, 1.0);
  } else {                     // brain pass
    if (vMask > 0.5) discard;
    outColor = vec4(1.0, 1.0, 1.0, uBrainAlpha);
  }
}
```

## 技术方法(总体简述)

- WebGL 渲染管线
  - `gl.enable(gl.DEPTH_TEST)` + `gl.depthFunc(gl.LEQUAL)`
  - `gl.enable(gl.BLEND)` + `gl.blendFunc(gl.SRC_ALPHA, gl.ONE_MINUS_SRC_ALPHA)`
- Shader（核心逻辑）
  - 顶点数据结构（每点 5 个 float）：
    - `aPosition.xyz`：归一化坐标
    - `aIntensity`：主体灰度（仅主体点用）
    - `aLabel`：标签值（0 表示非标签）
  - 片元着色器：
    - `uPass == 1` 仅绘制 `aLabel > 0` 的点，并按 label 分色。
    - `uPass == 0` 仅绘制非标签点，并使用 `uBrainAlpha` 控制透明度。
- 标签颜色（显眼配色）
  - label 1：亮红 `#ff3333`
  - label 2：亮青 `#00e6ff`
  - label 3：亮黄 `#fff200`
  - label 4：亮紫 `#e600ff`
- MHA 解析
  - 支持 `MET_SHORT / MET_USHORT / MET_INT / MET_FLOAT`。
  - 压缩数据优先使用 `DecompressionStream`，失败时回退到 `inflateRaw`。

# RAG Agent(协作医生判别)

可通过RAG对3D模型，模型可根据多模态数据进行辅助判别(热力图，3D模型，切面)

辅助医生注意到可能平时不注意的细枝末节的地方

同时可以帮助医生进行患者数据管理，系统操作

提供 /api/agent/chat 与 /api/agent/chat/stream 接口，支持同步与流式对话，便于前端快速接入与实时反馈，提升医生使用的
    流畅度与可感知性。
  - 以系统提示词驱动函数式 Agent，统一编排工具调用与对话记忆，保证推理路径可控、风格一致，降低幻觉风险，提升结果稳定性。
  - 基于 Chroma 向量库检索本地知识（RAG），以相似度阈值过滤无关内容，实现“先检索再生成”，显著提升专业回答的准确度与可追
    溯性。
  - 内置联网检索工具（DuckDuckGo HTML），为本地知识不足场景提供外部补充，扩大覆盖面并减少过时信息影响，增强回答的时效
    性。
  - 结合 Redis 管理用户会话与历史记录，支持多轮上下文与持续追问，改善交互连贯性，便于沉淀经验与形成可复用知识链。

后续：多智能体架构+自进化

# 架构

后端 整体变为微服务架构