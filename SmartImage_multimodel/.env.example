
# API 配置
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1 

# 模型配置(普通模型、视觉模型、嵌入模型)
MODEL_NAME=gpt-4o-mini  
IMAGE_MODEL_NAME=gpt-5-mini
EMBEDDING_MODEL_NAME = text-embedding-3-small

# 模型参数
MODEL_TEMPERATURE=0.2
MODEL_TIMEOUT=60

# 系统提示词配置
SYSTEM_PROMPT_PATH=./system_prompt.md
IMAGE_SYSTEM_PROMPT_PATH=./image_system_prompt.md

# Redis 配置（用于聊天历史）
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# RAG 配置
RAG_PERSIST_DIR=./data_storage/RAG/chroma_db
RAG_COLLECTION=zhiying_rag_demo
RAG_TOP_K=3
RAG_SCORE_THRESHOLD=0.25

# 聊天历史配置
CHAT_HISTORY_TTL_SECONDS=3600
CHAT_HISTORY_MAX_TOKENS=1200

# 聊天配置
SHOW_TOOL_CALLS = true

# 调试模式(是否在控制台输出debug级别的日志)（没啥用，因为很多的debug我写了之后解决问题了就立马删了）
DEBUG_MODE = false
